import SoapMataClient from "@/components/soap/SoapMataClient";

export const metadata = { title: "SOAP Mata v2 — Ophthalmology Suite" };

export default function SoapMataPage() {
  return <SoapMataClient />;
}
