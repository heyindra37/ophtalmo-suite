import TaperingMPClient from "@/components/tapering/TaperingMPClient";

export const metadata = { title: "Tapering MP — Ophthalmology Suite" };

export default function TaperingMPPage() {
  return <TaperingMPClient />;
}
